{
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "{{data.group_folder}}/{{data.path}}/src",
  "projectType": "application",
  "targets": {
    "build": {
      "dependsOn": [
        {
          "target": "env",
          "projects": "self"
        }
      ],
      "executor": "@nrwl/node:webpack",
      "outputs": [
        "{options.outputPath}"
      ],
      "options": {
        "outputPath": "dist/{{data.group_folder}}/{{data.path}}",
        "main": "{{data.group_folder}}/{{data.path}}/src/main.ts",
        "tsConfig": "{{data.group_folder}}/{{data.path}}/tsconfig.app.json",
        "assets": [],
        "webpackConfig": [
          "{{data.group_folder}}/{{data.path}}/webpack.js"
        ]
      },
      "configurations": {
        "production": {
          "optimization": true,
          "extractLicenses": true,
          "inspect": false
        }
      }
    },
    "serve": {
      "dependsOn": [
        {
          "target": "env",
          "projects": "self"
        }
      ],
      "executor": "@nrwl/node:node",
      "options": {
        "buildTarget": "{{data.path}}:build"
      }
    },
    "lint": {
      "dependsOn": [
        {
          "target": "env",
          "projects": "self"
        }
      ],
      "executor": "@nrwl/linter:eslint",
      "outputs": [
        "{options.outputFile}"
      ],
      "options": {
        "lintFilePatterns": [
          "{{data.group_folder}}/{{data.path}}/**/*.ts"
        ]
      }
    },
    "test": {
      "dependsOn": [
        {
          "target": "env",
          "projects": "self"
        }
      ],
      "executor": "@nrwl/jest:jest",
      "outputs": [
        "coverage/{{data.group_folder}}/{{data.path}}"
      ],
      "options": {
        "jestConfig": "{{data.group_folder}}/{{data.path}}/jest.config.ts",
        "passWithNoTests": true
      }
    },
    "env": {
      "executor": "nx:run-commands",
      "options": {
        "command": "seki env generate -p={{data.path}}",
        "cwd": "."
      }
    }
  },
  "tags": []
}
